---
const { title } = Astro.props;
---

<section class="px-16">
    <div class="w-full mx-auto max-w-[1200px] my-12">
        {title && <h2 class="text-4xl">{title}</h2>}
        <slot />
    </div>
</section>
