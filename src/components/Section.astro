---
const { title, id, class: customClass } = Astro.props;
---

<section class={`px-8 ${customClass || ''}`} id={id}>
    <div class="w-full mx-auto max-w-screen-xl pt-16">
        {title && <h2 class="text-4xl">{title}</h2>}
        <slot />
    </div>
</section>
